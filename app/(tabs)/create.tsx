import React from 'react';
import { View } from 'react-native';

// Placeholder route used only for rendering the center "+" tab button.
export default function CreatePlaceholderScreen() {
  return <View />;
}
